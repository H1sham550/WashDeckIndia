DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"
SESSION_SECRET="replace-with-a-long-random-secret"
APP_URL="http://localhost:3000"

# Optional. If omitted, OTPs are printed in the server console for local development.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="WashDeck <no-reply@washdeck.local>"
